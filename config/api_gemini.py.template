"""
Gemini API configuration for multi-agent oncology feature extraction.

SETUP INSTRUCTIONS:
1. Copy this file to 'api_gemini.py' (without .template extension)
2. Fill in your own Google Cloud project credentials below
3. Ensure you have set up Google Cloud authentication (gcloud auth application-default login)
4. Ensure 'api_gemini.py' is in your .gitignore to prevent committing credentials
"""

import vertexai
from vertexai.generative_models import GenerativeModel

# Vertex AI Configuration
# Replace with your Google Cloud Project ID
PROJECT_ID = "YOUR_GCP_PROJECT_ID_HERE"

# Replace with your preferred Google Cloud region
# Common options: "us-central1", "us-east5", "europe-west1"
LOCATION = "us-central1"

vertexai.init(project=PROJECT_ID, location=LOCATION)

# Initialize Gemini models
gemini_model = GenerativeModel(model_name="gemini-2.5-flash")
gemini_model_thinking = GenerativeModel(model_name="gemini-2.5-pro")

# Provider name (do not change)
llm_provider = "gemini"

# Model identifier (not used directly by Gemini, but kept for consistency)
model = "gemini-2.0-flash-exp"
